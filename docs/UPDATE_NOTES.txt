Обновлено:
- процесс собирается как AvastSvc.exe;
- приветственное сообщение: «Запуск успешен.»;
- команда /queue;
- команда /stop;
- BOT_TOKEN, CHAT_ID и RELAY_SECRET из исходного архива не удалялись и не заменялись.
- build.bat автоматически использует client/icon.ico, если такой файл есть.

Для работы новых команд нужно заменить server.py на VDS и перезапустить telegram-relay.
После этого пересобрать клиент новым build.bat.
